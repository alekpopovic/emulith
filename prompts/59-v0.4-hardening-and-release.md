# Task 59 — Emulith v0.4 three-cloud hardening and release readiness

Target agent: Codex with GPT-5.6  
Prerequisite: Tasks 00–58.

Perform the final hardening, migration, documentation, and release-readiness pass for Emulith `v0.4.0`.

Do not publish, tag, push, or open a release.

## Required workflow

1. Run the complete baseline suite before edits.
2. Inspect all AWS, Azure, and GCP providers, listeners, protocol handlers, migrations, snapshot logic, generated compatibility data, Docker files, workflows, release tooling, and docs.
3. Produce a concise prioritized findings list:
   - release blocker;
   - high;
   - medium;
   - low.
4. Immediately fix all release blockers and high-severity issues within scope.
5. Add focused regression tests for every defect fixed.
6. Run all acceptance gates.
7. End with an evidence-based `GO` or `NO-GO` for public `v0.4.0`.

Do not stop after the audit.

## Intended release scope

### AWS tested subset

```text
STS
S3
SQS
DynamoDB
SNS
CloudWatch Logs
```

### Azure tested subset

```text
Blob Storage
Queue Storage
Table Storage
```

### GCP tested subset

```text
Pub/Sub
Cloud Storage
Firestore
```

No serverless functions, Kubernetes, dashboard, production deployment, strict IAM/auth, or complete cloud parity.

## Listener lifecycle

Audit all seven default service ports plus shared admin behavior:

```text
4566
10000
10001
10002
8085
8080
9023
```

Verify:

- partial startup rollback;
- graceful HTTP/gRPC shutdown;
- StreamingPull drain/cancel behavior;
- in-flight upload handling;
- shared store closes once;
- health accuracy;
- ephemeral test ports;
- container signals;
- no goroutine/listener leak;
- clear port-conflict diagnostics;
- race-free repeated start/stop.

## Pub/Sub hardening

Verify/fix:

- resource parsing;
- topic/subscription lifecycle;
- atomic publish fan-out;
- binary/attribute fidelity;
- unary pull;
- ack ID rotation;
- deadlines/redelivery;
- competing consumers;
- StreamingPull flow control;
- slow clients/backpressure;
- reconnect;
- ordering keys;
- subscription deletion during stream;
- restart/export/import;
- no goroutine leak;
- bounded message/batch/stream memory.

Keep unsupported:

```text
push
exactly-once
dead-letter
seek/snapshots
schemas
cross-project
production scheduling guarantees
```

## Cloud Storage hardening

Verify/fix:

- bucket/object validation;
- path/symlink safety;
- media/multipart/resumable uploads;
- session recovery/expiration;
- chunk offsets/retries;
- atomic finalization;
- MD5/CRC32C;
- generation/metageneration;
- preconditions;
- ranges;
- listing/delimiter/tokens;
- restart/snapshot;
- bounded MIME/JSON/request sizes;
- no orphan body/session files.

Keep unsupported:

```text
IAM/ACL enforcement
signed URL verification
versioning history
compose/rewrite/copy
lifecycle
retention lock
soft delete
```

## Firestore hardening

Verify/fix:

- protobuf size/depth limits;
- document/field path parsers;
- Int64/double/NaN/timestamp/bytes fidelity;
- CRUD/masks/preconditions;
- transforms;
- atomic Commit;
- BatchWrite semantics;
- transaction conflict/retry;
- transaction expiration;
- ListDocuments tokens;
- RunQuery filters/order/cursors;
- streaming response cancellation;
- restart/snapshot;
- bounded query scans;
- fuzz/race coverage.

Keep unsupported:

```text
Listen/watch
aggregation
admin/index APIs
Datastore mode
named databases
full production index enforcement
```

## Cross-cloud isolation and workflow

Run one public-SDK-only workflow:

1. create S3 bucket, Azure Blob container, and GCS bucket with the same logical name;
2. upload different objects to all three;
3. create SQS, Azure Queue, and Pub/Sub resources with overlapping names;
4. send/receive/ack messages;
5. create DynamoDB table, Azure Table, and Firestore documents;
6. publish SNS-to-SQS;
7. write CloudWatch Logs;
8. keep one Pub/Sub message in flight;
9. stop Emulith;
10. restart with the same volume;
11. verify durable state;
12. export snapshot;
13. reset;
14. verify resources are absent;
15. import;
16. verify all providers through official SDKs.

No direct database assertions may replace public client verification.

## Migration evidence

Use authentic repository fixtures:

- v0.1 where still supported;
- v0.2;
- v0.3 -> v0.4;
- old snapshots;
- failed migration rollback;
- unsupported future schema/snapshot rejection;
- no AWS/Azure data loss;
- correct empty GCP state after v0.3 migration.

Prepare/update:

```text
docs/upgrade-v0.3-to-v0.4.md
```

## Security and robustness

Audit:

- no auth/token/API-key/signed-query logging;
- no payload logging;
- no real-cloud fallback;
- no ADC/metadata/ambient credentials;
- loopback guards in tests/demos;
- bounded HTTP/gRPC/protobuf/JSON/MIME/XML/form/archive parsers;
- panic recovery;
- safe request IDs/errors;
- filesystem/archive traversal protection;
- admin endpoints documented as trusted-local only;
- non-root container/volume permissions;
- least-privilege CI;
- no secrets;
- dependency licenses;
- SBOM/checksum/signing configuration;
- no telemetry/phone home.

## Fuzzing and leak tests

Maintain bounded seed fuzz targets for:

```text
GCP resource names
Pub/Sub protobuf/stream request validation
Storage upload routing/Content-Range/MIME/page tokens
Firestore Value/field/document paths/query AST/cursors
snapshot archive
cross-provider router classification
```

Add stream/goroutine leak tests for Pub/Sub and gRPC shutdown.

Document longer maintainer fuzz commands without making standard CI excessive.

## Compatibility truthfulness

Verify:

- each `supported` operation has a passing official client test;
- `partial` entries list exact deviations;
- SDK module/API versions are reported;
- generated docs are current;
- no skipped mandatory test;
- AWS/Azure statuses do not regress silently;
- README summary matches generated matrix;
- permissive auth/no-security-rules behavior is prominent.

Downgrade status rather than overstate support.

## Performance sanity

Measure without brittle gates:

- startup with seven listeners;
- idle memory;
- Pub/Sub batches/streaming;
- multi-chunk GCS upload;
- object listing;
- several thousand Firestore documents and queries;
- mixed three-cloud workload;
- snapshot export/import.

Fix obvious quadratic/unbounded behavior. Record methodology, not marketing claims.

## Docker and release artifacts

Validate:

- all ports;
- non-root UID;
- persistent volume;
- graceful stop;
- multi-arch build;
- version metadata;
- OCI labels;
- checksums;
- SBOM;
- release snapshot;
- signing/provenance config;
- no publishing from pull requests.

## Documentation and changelog

Prepare:

```text
CHANGELOG.md
README.md
docs/releases.md
docs/architecture.md
docs/state-format.md
docs/upgrade-v0.3-to-v0.4.md
generated compatibility docs
```

Release notes must state:

- exact service subsets;
- local development/CI-only use;
- permissive/no-IAM auth;
- all ports/environment variables;
- migration behavior;
- known limitations;
- no account/license key/forced telemetry;
- AGPL-3.0-or-later;
- experimental interfaces.

Use `v0.4.0` only in release preparation. Do not tag or publish.

## Acceptance gate

Run every applicable command:

```bash
gofmt -w <changed-go-files>
go test ./...
go test -race ./...
go vet ./...
make build
make compatibility
make compatibility-report
make compatibility-check
make demo
make demo-azure
make demo-gcp
make demo-multicloud
make docker-build
make release-snapshot
make release-check
```

When Docker is available:

1. build the image;
2. run as non-root with persistent volume;
3. verify all listeners;
4. execute the three-cloud SDK workflow;
5. restart;
6. verify persistence;
7. export/reset/import;
8. stop and clean up.

## Final report

Include:

- prioritized findings/fixes;
- regression tests;
- migration evidence;
- compatibility summary by provider/service;
- exact acceptance outcomes;
- remaining limitations;
- release blockers;
- explicit `GO` or `NO-GO`.

A `GO` is allowed only when all mandatory checks not blocked by the environment pass and no known data-loss, security-boundary, goroutine-leak, migration, or compatibility-report blocker remains.


## Execution contract

You are the implementation agent for this task. Complete the work in the current Emulith repository; do not stop after writing a plan.

1. Read every applicable `AGENTS.md` before changing files.
2. Inspect the repository, provider registry, listener lifecycle, migrations, tests, compatibility catalog, generated documentation, Docker setup, and dependency versions.
3. Run the relevant baseline checks before editing when practical.
4. State a concise implementation plan, then execute it immediately.
5. Make reasonable non-blocking assumptions instead of asking for confirmation.
6. Keep the change scoped to this task. Do not implement later roadmap items.
7. Preserve all existing AWS and Azure behavior and compatibility unless this task explicitly fixes a defect.
8. Prefer provider-specific protocol implementations over a false universal cloud abstraction.
9. Never use Google Cloud emulators, LocalStack, Azurite, Moto, MinIO, ElasticMQ, or another emulator as an Emulith runtime dependency.
10. Never contact real GCP, AWS, or Azure endpoints. All tests must be hermetic and loopback-only.
11. Do not use Application Default Credentials, `gcloud` user credentials, service-account files, metadata-server probing, workload identity, or ambient cloud profiles in compatibility tests.
12. Do not add accounts, entitlement checks, license keys, forced telemetry, analytics, or phone-home behavior.
13. Do not commit, push, tag, publish a release, or open a pull request.
14. Bound all parsers, protobuf/JSON bodies, gRPC messages, stream buffers, page sizes, upload sessions, query plans, and allocations derived from untrusted input.
15. Never log authorization headers, OAuth tokens, API keys, signed URLs, object bodies, Pub/Sub message data, Firestore document data, or other user payloads.
16. Keep request IDs, error mapping, and compatibility claims deterministic and test-backed.
17. Format changed files and run every verification command applicable to the repository.
18. Fix failures caused by your changes. If the environment blocks a command, report the exact limitation and run the closest safe verification.
19. Update compatibility documentation only for behavior backed by executable official-client compatibility tests.
20. Finish with:
    - implementation summary;
    - important design decisions;
    - changed files;
    - exact commands run and outcomes;
    - compatibility status changes;
    - genuine remaining limitations.

Emulith remains a development/CI emulator, not a production service.
