# Task 74 — Emulith v0.5 serverless hardening and release readiness

Target agent: Codex with GPT-5.6  
Prerequisite: Tasks 00–73.

Perform the final hardening, migration, compatibility, documentation, and release-readiness pass for Emulith `v0.5.0`.

Do not publish, tag, push, or open a release.

## Required workflow

1. Run the complete baseline suite before edits.
2. Inspect all cloud providers, function runtimes, Docker integration, builders, warm pools, durable delivery, triggers, schedulers, observability, migrations, snapshots, compatibility reports, examples, CI, Docker image, release tooling, and docs.
3. Produce a concise prioritized findings list:
   - release blocker;
   - high;
   - medium;
   - low.
4. Immediately fix every release blocker and high issue within scope.
5. Add focused regression tests for every defect fixed.
6. Run all acceptance gates.
7. End with an evidence-based `GO` or `NO-GO` for public `v0.5.0`.

Do not stop after the audit.

## Intended release scope

### Existing cloud-service subsets

```text
AWS: STS, S3, SQS, DynamoDB, SNS, CloudWatch Logs
Azure: Blob, Queue, Table
GCP: Pub/Sub, Cloud Storage, Firestore
```

### Serverless subset

```text
OCI/Docker local execution
AWS Lambda image custom runtime + Runtime API
AWS SQS/SNS/S3/EventBridge/Scheduler triggers
Azure custom-handler HTTP/Timer/Queue/Blob/Event Grid subset
GCP Functions Framework HTTP/CloudEvent + Pub/Sub/Storage/Firestore/Eventarc/Scheduler subset
durable retries and DLQ
local logs/traces/metrics
```

No full managed language runtimes, hostile multi-tenant sandbox, production IAM, Kubernetes runtime, dashboard, or complete provider parity.

## Runtime security audit

Verify/fix:

```text
Docker daemon trust boundary clearly documented
no privileged containers
no host PID/IPC/network default
no Docker socket mount into function containers
no arbitrary bind mounts/devices
non-root enforcement or explicit safe exception
read-only root where possible
isolated writable /tmp
capability drop/no-new-privileges
memory/CPU/PIDs/log limits
network mode behavior truthful
image digest/revision isolation
orphan cleanup only for owned containers
build context traversal/symlink/device rejection
build log/secret redaction
no implicit registry pull
```

Do not claim isolation stronger than evidence.

## Execution correctness audit

Verify/fix:

- cold/warm state machine;
- concurrent acquisition;
- per-function/global limits;
- timeout/cancellation;
- init/runtime errors;
- container crash;
- client disconnect;
- revision drain;
- graceful shutdown;
- daemon restart;
- missing image;
- no invocation falsely succeeded;
- no event lost before source acknowledgement;
- no duplicate simultaneous attempt;
- no goroutine/container/file leak.

## Durable delivery audit

Verify/fix:

- lease transaction;
- retry/backoff/jitter;
- deadline;
- attempt counts;
- event/invocation ID stability;
- DLQ retry;
- scheduler restart;
- cancellation;
- reset/export/import coordination;
- fair concurrency;
- at-least-once documentation;
- no exactly-once language;
- payload checksum/size/retention.

## AWS serverless audit

Verify/fix:

```text
Lambda control-plane SDK behavior
Runtime API headers/lifecycle
RequestResponse/Event/DryRun
LogType Tail
CloudWatch Logs integration
SQS partial batch
SNS event envelope
S3 notifications/filter/encoding
EventBridge pattern/targets
scheduler
retry/DLQ
```

DynamoDB Streams must remain unsupported/experimental unless fully tested.

## Azure serverless audit

Verify/fix:

```text
custom-handler envelope
HTTP routing and response
timer parser/past due/restart
Queue visibility/pop receipt/poison
Blob finalized/overwrite/delete
output bindings
Event Grid and CloudEvents schema
filter/retry/dead-letter
```

State prominently that full Azure Functions Host/language workers are not emulated.

## GCP serverless audit

Verify/fix:

```text
HTTP function proxy behavior
CloudEvents binary/structured
Pub/Sub ack/redelivery/order
Storage generation events
Firestore old/new/update mask
Eventarc trigger/filter
Scheduler cron/manual/pause
concurrency/retry/DLQ
```

State prominently that full Cloud Functions/Cloud Run control planes are not emulated.

## Observability/privacy audit

Verify/fix:

- payload/secret redaction;
- log attribution;
- truncation/retention;
- no unbounded follow buffer;
- W3C trace propagation;
- retry keeps trace/new span;
- metric label cardinality;
- OTLP disabled by default;
- explicit loopback endpoint;
- no telemetry/network when disabled;
- CloudWatch integration truthfulness;
- no Application Insights/Cloud Logging parity claim.

## Migration and snapshot evidence

Use authentic fixtures:

```text
v0.1
v0.2
v0.3
v0.4 -> v0.5
```

Verify:

- all existing cloud state preserved;
- function definitions/revisions/triggers migrate;
- missing image behavior;
- queued/retry/DLQ state round trip;
- scheduler next-run behavior;
- active-running container not serialized;
- import activation blocks leasing until complete;
- failed migration/import rolls back;
- future schema rejected;
- no downgrade.

Prepare/update:

```text
docs/upgrade-v0.4-to-v0.5.md
```

## Final multicloud serverless scenario

Use public SDKs/protocols and real local fixture images:

1. Create S3 bucket and Lambda function.
2. Configure S3 notification.
3. Upload object; verify Lambda runs.
4. Lambda writes an SQS message.
5. SQS mapping invokes another Lambda with partial-batch behavior.
6. Create Azure Blob container and custom-handler function.
7. Upload blob; verify Azure Blob trigger.
8. Function writes Azure Queue/Table output bindings.
9. Publish Pub/Sub; verify GCP CloudEvent function.
10. Write Firestore document; verify Firestore trigger.
11. Exercise EventBridge, Event Grid, and Eventarc.
12. Run one scheduler job per provider.
13. Intentionally fail a function; verify retry.
14. Exhaust attempts; verify DLQ.
15. Restart Emulith during retry wait.
16. Verify queued/retry work resumes.
17. Inspect logs, traces, and metrics.
18. Export snapshot.
19. Reset.
20. Import.
21. Verify definitions, schedules, queued state, and all cloud resources.
22. Verify missing/available image behavior deterministically.

No direct SQL assertions may replace public/API verification.

## Chaos and fuzzing

Run/maintain bounded coverage for:

```text
Docker unavailable/restart
container startup/readiness failure
runtime protocol malformed response
log flood
oversized event/response
build context parser
Lambda Runtime API paths
Azure custom-handler envelope/routes
CloudEvents parser
cron parsers
EventBridge/Event Grid/Eventarc filters
delivery queue state transitions
snapshot/archive
shutdown/reset/export races
```

Run race and goroutine leak checks.

## Compatibility truthfulness

Verify:

- every `supported` function/control-plane/trigger claim has a passing official SDK/protocol/e2e test;
- partial entries list exact deviations;
- runtime type and event envelope are identified;
- no skipped mandatory test;
- generated docs are current;
- existing cloud compatibility does not regress silently;
- README does not imply full provider function parity;
- Docker security boundary is prominent.

Downgrade status rather than overstate.

## Performance sanity

Measure without brittle CI thresholds:

- startup with cloud listeners and worker engine;
- idle memory with no functions;
- cold start/warm invocation;
- concurrent HTTP invocations;
- batch event throughput;
- retry queue scale;
- log retention;
- many idle schedules;
- mixed cloud/serverless workload;
- snapshot export/import.

Fix obvious unbounded/quadratic behavior. Record methodology, not marketing claims.

## Docker and release artifacts

Validate:

- Emulith image remains non-root where possible;
- runtime socket/endpoint configuration;
- function fixture images;
- persistent volumes;
- graceful stop;
- multi-arch release configuration;
- version metadata;
- OCI labels;
- checksums;
- SBOM includes serverless dependencies;
- release snapshot;
- signing/provenance config;
- no publishing from pull requests;
- no secrets.

## Documentation and changelog

Prepare:

```text
CHANGELOG.md
README.md
docs/releases.md
docs/architecture.md
docs/serverless-architecture.md
docs/serverless-security.md
docs/functions-manifest.md
docs/state-format.md
docs/upgrade-v0.4-to-v0.5.md
generated compatibility docs
```

Release notes must state:

- exact cloud and serverless subsets;
- development/CI-only scope;
- Docker daemon trust boundary;
- no strong multi-tenant isolation;
- image/custom-handler/Functions Framework limitations;
- permissive/no-IAM auth;
- migration behavior;
- no account/license key/forced telemetry;
- AGPL-3.0-or-later;
- experimental interfaces.

Use `v0.5.0` only in preparation. Do not tag or publish.

## Acceptance gate

Run every applicable command:

```bash
gofmt -w <changed-go-files>
go test ./...
go test -race ./...
go vet ./...
make build
make compatibility
make compatibility-report
make compatibility-check
make demo
make demo-azure
make demo-gcp
make demo-multicloud
make demo-serverless
make demo-multicloud-serverless
make docker-build
make release-snapshot
make release-check
```

When Docker is available:

1. build fixture and Emulith images;
2. run Emulith with explicit trusted runtime access;
3. verify cloud listeners and runtime health;
4. execute the full serverless scenario;
5. inspect cleanup/non-root/resource limits;
6. restart;
7. verify durable delivery;
8. export/reset/import;
9. stop and remove only managed resources.

## Final report

Include:

- prioritized findings and fixes;
- regression tests added;
- migration/snapshot evidence;
- compatibility summary by provider/runtime/trigger;
- runtime security findings;
- exact acceptance outcomes;
- remaining limitations;
- release blockers;
- explicit `GO` or `NO-GO`.

A `GO` is allowed only when all mandatory checks not blocked by the environment pass and no known data-loss, sandbox-boundary misrepresentation, credential leak, orphan-container, delivery-state, migration, or compatibility-report blocker remains.


## Execution contract

You are the implementation agent for this task. Complete the work in the current Emulith repository; do not stop after writing a plan.

1. Read every applicable `AGENTS.md` before changing files.
2. Inspect the repository, provider registry, listener lifecycle, state schema, migration history, SDK compatibility suite, generated compatibility catalog, Docker setup, release tooling, and documentation.
3. Run the relevant baseline checks before editing when practical.
4. State a concise implementation plan, then execute it immediately.
5. Make reasonable non-blocking assumptions instead of asking for confirmation.
6. Keep the change scoped to this task. Do not implement later roadmap items.
7. Preserve all existing AWS, Azure, and GCP behavior and compatibility unless this task explicitly fixes a defect.
8. Keep provider-specific control planes, invocation envelopes, and event semantics separate. Do not create a false universal cloud-function API.
9. Never depend on LocalStack, Azurite, Google emulators, SAM Local, Azure Functions Core Tools, Functions Framework emulators, Moto, MinIO, ElasticMQ, or another emulator as an Emulith runtime dependency.
10. Never contact real AWS, Azure, GCP, registries, metadata services, or public cloud endpoints during tests. All compatibility and end-to-end tests must be hermetic and loopback-only.
11. Do not use ambient cloud credentials, Docker registry credentials, user profiles, ADC, managed identity, instance metadata, or default credential chains in tests.
12. Do not add accounts, entitlement checks, license keys, forced telemetry, analytics, or phone-home behavior.
13. Do not commit, push, tag, publish a release/image, or open a pull request.
14. Bound all request bodies, event payloads, logs, build contexts, archive inputs, queues, retries, streams, concurrency, and allocations derived from untrusted input.
15. Never log secrets, authorization headers, environment-variable values marked sensitive, event payloads, queue messages, object contents, function request bodies, or function responses by default.
16. Treat access to a Docker/OCI daemon as a trusted local security boundary. Do not claim strong multi-tenant isolation.
17. Keep request IDs, invocation IDs, event IDs, error mapping, and compatibility claims deterministic and test-backed.
18. Format changed files and run every verification command applicable to the repository.
19. Fix failures caused by your changes. If the environment blocks Docker or another command, report the exact limitation and run the closest safe verification using fakes/in-process tests.
20. Update compatibility documentation only for behavior backed by executable SDK or protocol compatibility tests.
21. Finish with:
    - implementation summary;
    - important design decisions;
    - changed files;
    - exact commands run and outcomes;
    - compatibility status changes;
    - security-boundary notes;
    - genuine remaining limitations.

Emulith remains a development/CI emulator, not a production service.
