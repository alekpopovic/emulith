# Experimental manifest

With Emulith running locally:

```bash
emulith apply -f examples/manifests/aws-basic/emulith.yaml
```

The `version: 1` schema is experimental for `v0.1.0-poc`. Resources are applied in order through the public AWS-compatible SDK endpoints.
